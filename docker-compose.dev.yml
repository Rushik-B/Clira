services:
  app:
    build: .
    image: clira-app:local
    pull_policy: never

  worker:
    image: clira-app:local
    pull_policy: never

  backfill-worker:
    image: clira-app:local
    pull_policy: never

  gmail-pull-worker:
    image: clira-app:local
    pull_policy: never

  cron:
    image: clira-app:local
    pull_policy: never

  db:
    ports:
      - "15432:5432"

  redis:
    ports:
      - "16379:6379"
